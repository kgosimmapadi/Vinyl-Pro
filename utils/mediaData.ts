
import { Track, Video } from '../types';

// PRODUCTION MODE:
// Mocks removed. Data is populated dynamically via File System / Electron IPC.

export const SCANNED_MUSIC: Track[] = [];
export const SCANNED_VIDEOS: Video[] = [];
